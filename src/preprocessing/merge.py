"""Preserve the legacy preprocessing-merge module path.

Purpose:
    Maintain compatible imports without creating another feature or data merge
    implementation.
Responsibility:
    Act as a documented placeholder; it performs no transformations.
Inputs:
    None.
Outputs:
    No runtime output.
Interactions:
    Active feature composition occurs in ``src.features.feature_pipeline`` and
    ``src.features.feature_builder``.
"""
