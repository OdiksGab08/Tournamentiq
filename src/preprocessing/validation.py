"""Preserve the legacy preprocessing-validation module path.

Purpose:
    Maintain compatible imports without duplicating active validation logic.
Responsibility:
    Act as a documented placeholder; it performs no validation work.
Inputs:
    None.
Outputs:
    No runtime output.
Interactions:
    Active data checks are implemented in ``src.data.validate_results`` and
    ``src.data.validation.integrity_checks``.
"""
