"""Preserve the legacy prediction-pipeline module path.

Purpose:
    Maintain compatibility for historical imports without reimplementing live
    model inference.
Responsibility:
    Serve as a documented placeholder; it contains no prediction behavior.
Inputs:
    None.
Outputs:
    No runtime output.
Interactions:
    Active prediction flows use ``src.simulator.predictor`` through the
    dashboard match-prediction service.
"""
